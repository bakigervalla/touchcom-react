import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  timeSelectionContainer: { alignItems: 'center', gap: 10, flexWrap: 'wrap' },
  moreButton: { cursor: 'pointer' },
}));
