import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  configurationContainer: { flexDirection: 'column', gap: 32, width: 576 },
  contentContainer: { gap: 28, paddingBottom: 28 },
}));
