import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  residentFormContainer: { flexDirection: 'column', gap: 20, width: '100%' },
  deviceSearch: { marginLeft: 'auto' },
}));
