import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  companyFormContainer: { flexDirection: 'column', gap: 20, width: '100%' },
  deviceSearch: { marginLeft: 'auto' },
}));
