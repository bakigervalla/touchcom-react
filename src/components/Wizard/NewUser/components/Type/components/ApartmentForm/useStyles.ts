import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  apartmentFormContainer: { flexDirection: 'column', gap: 20, width: '100%' },
  deviceSearch: { marginLeft: 'auto' },
}));
